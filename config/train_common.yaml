training:
  epochs: 100
  batch_size: 8
  workers: 4

  optimizer:
    type: adamw
    lr: 0.0005
    weight_decay: 0.0005

  scheduler:
    type: cosine
    warmup_epochs: 3

  logging:
    log_interval: 20
    save_interval: 10

  output_dir: experiments/phase1
